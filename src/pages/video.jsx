import React from "react";
import Layout from "@/template/Layout";

const Video = () => {
  return (
    <Layout>
      {(commonData) => (
        <>
          <div>Video</div>
        </>
      )}
    </Layout>
  );
};

export default Video;
